# 使用说明（中文）

## 〇、网页版（老师打开链接即可用，无需 MATLAB）

网页在 `web/` 文件夹。本地预览：

```bash
cd web && python3 -m http.server 8765
```

然后打开 <http://localhost:8765>。直接双击 `web/index.html` 也能用。

六个页签对应老师的要求：**Simulator**（外耳／中耳／组合的幅度、相位、中耳输入阻抗，38 个滑块实时重算）、
**Experiments**（三组参数实验，先预测→再看AI解释→运行→记录，可导出）、
**Child / Otitis media**（儿童基线与中耳积液）、
**Circuit & sources**（五个子系统、连接关系、参数来源分类、级联假设、局限）、
**Validation & AI log**（幅度与相位验证、AI 纠错记录）、
**Audio & export**（试听与下载）。

网页的计算是**实时算的，不是预先生成的图片**：所有曲线由 `web/js/` 里的公式当场计算，
并与 MATLAB 逐点对拍（见下）。

## 一、怎么跑 MATLAB 版

在 MATLAB 里打开 `提交_SUBMIT/code` 文件夹，敲一个词：

```matlab
runAll
```

它会依次跑完 4 套验证、生成全部 5 张图、跑 4 个参数实验和中耳炎扩展，最后把图全部弹出来。图同时存进 `figures/`。

也可以双击 `code/run_middle_ear.command`（自动启动 MATLAB 并运行 `runAll`）。

想开交互界面：`combinedEarModel`

> ⚠️ `Z_cavity`、`Z_eardrum`、`Z_ossicles`、`Z_joint`、`Z_stapes`、`Z_cochlea` 这六个是**需要传参数的函数**（要 `(f, p2)`），直接敲名字会报错。你不需要手动调它们。

## 二、四套验证分别验什么

| 命令 | 验什么 | 结果 |
|---|---|---|
| `test_outerEar` | **H1 原样继承**的外耳自测（20 项，含与 ngspice 的交叉验证） | 全部通过 |
| `test_vsPaper` | 与论文 Fig.2、Fig.3 原图对照 | 幅度 1.34 / 0.91 dB，相位 7.9° / 2.5° |
| `test_middleEar` | 电路代数、单位、变压器、响应形状 | 80 项，0 失败 |
| `test_audioIR` | 音频链路是否等于解析传函 | 23 项，0 失败 |
| `test_combinedGui` | 界面能否构建、控件是否都能用 | 0 失败 |

源文件全部 `checkcode` 无警告。

### 和 H1 的关系

`outerEarParams.m`、`outerEarResponse.m`、`outerEarIR.m`、`test_outerEar.m` 四个文件**逐字节原样继承**，没有改写。`runAll` **第一步就先跑 H1 自己的测试套件**，确认这份拷贝在新环境里行为不变，然后才往上接中耳——不是假定它还对，是验过。

H1 的 GUI／出图脚本没有搬过来，因为 H2 的对应文件驱动的是不同的传递函数，另写了。

## 三、老师要求 → 对应文件

| 作业要求 | 在哪里 | 关键结果 |
|---|---|---|
| **代码** | `code/` 全部 20 个 .m 文件 | — |
| 各子系统阻抗函数 | `Z_cavity/Z_eardrum/Z_ossicles/Z_joint/Z_cochlea.m`，另加 `Z_stapes.m` | 论文图里镫骨段是独立一块，故拆为 6 个 |
| **外耳单独响应** | `figures/fig1_outer_ear.png` | 峰 +22.0 dB @2973 Hz |
| **中耳单独响应** | `figures/fig2_middle_ear.png` 中panel | 峰 +20.4 dB @1422 Hz |
| **组合响应** | `figures/fig3_combined.png` | 峰 +39.2 dB @3003 Hz |
| **中耳输入阻抗** | `fig2` 上panel；`R2.inputImpedance` | 100 Hz 3.16e8，1 kHz 6.99e7 Pa·s/m³ |
| **幅度与相位** | fig1/fig2/fig3 每张都有相位子图 | 见下方第四节 |
| **三组参数实验（预测 vs 结果）** | `runMiddleEarExperiments.m`；`fig4_experiments.png`；报告 §5 | 做了 4 组：质量 / 顺性 / 阻性 + 对照 |
| **电路验证说明** | 报告 §2、§3；`test_vsPaper.m` | 三条独立算术核对 + 与论文原图对照 |
| **参数来源分类** | 报告 §3 表 + `middleEarParams.m` 头部 | 3 项已独立验算，其余逐条标注 |
| **三项局限** | 报告 §6 | 级联负载、集总失效、边界条件不一致 |
| **AI 使用与纠错记录** | 报告 §10 | 6 条，含 4 次真实纠错 |
| **研究生扩展（小儿中耳炎）** | `runOtitisMedia.m`；`fig5_otitis_media.png`；报告 §8 | **标为探索性，且明确未能复现临床量级** |

## 四、幅度验证了什么，相位验证了什么

这两件事**不能混为一谈**：

- **幅度**（Fig.3 1.34 dB rms，Fig.2 0.91 dB rms）验证的是**元件数值和变压器折算**。它对拓扑不敏感——好几种错误接法都能凑出差不多的幅度曲线。我之前那版错误电路的幅度看起来也"还行"。
- **相位**（Fig.3 7.9° rms，Fig.2 2.5° rms）验证的是**拓扑和先后顺序**。相位由信号依次经过几个独立储能元件、按什么顺序决定，所以能抓出"接错节点""串并联搞反"这类幅度掩盖得住的错误。

**只有两者同时通过，才说明零件对、接法也对。**

## 五、还没做完的（如实列出）

1. **中耳炎扩展没有复现临床量级**。预测损失 1.0 / 3.1 / 7.1 dB，临床气骨导差是 10–40 dB。原因已定位（报告 §8）：`R_cm` 并联在腔体两端，把腔体阻抗上限锁死在 420 Ω，所以无论积液排掉多少空气，电路几乎感觉不到。这是 Fig.1 画法本身的性质，不是倍率没调好——**要修得换元件，不是换数字**。
2. **儿童中耳腔容积 2.8 cm³ 没有来源**。查到的都是外耳道等效容积（0.4–1.3 cm³），那是另一个腔，不能替代。这是扩展里最大的不确定来源。
3. **Eq.7 单位问题未定论**。已按"两种读法 + 各自证据"写清，没有断言论文印错。要了结需核对 Lutman & Martin (1979) 原文。
4. **第二篇论文（25 页扫描件）没读**。无文字层，需装 `poppler` 才能整页渲染。所以 1979 那篇的 Table 1 / Figure 12 矛盾（R_D2 = 12 或 220 Ω，C_C = 0.65 或 0.6 μF）**仍未了结**——代码里没有这两个元件，我没有静默选值。
5. **外耳段本身就偏高，级联继承了这个偏差**。H1 的组合峰值约 +22 dB @3 kHz，真实人耳在 2.5–3 kHz 是 +15～+20 dB，即高估约 2–7 dB。H2 的 +39.2 dB 把这个偏差原样带了进来，引用绝对数值时要扣掉。
6. **非线性部分未实现**。Eq.3/6/7 已在 `middleEarParams.m` 注释存档；`p2.nonlinear = true` 会明确报错，不会悄悄返回线性结果。
